import {
    FolderKanban,
    StickyNote,
    BookOpen,
    CheckCircle
} from "lucide-react";

export const ICON_MAP = {
    totalProjects: FolderKanban,
    totalQuickNotes: StickyNote,
    totalLearnings: BookOpen,
    totalHabits: CheckCircle
};
